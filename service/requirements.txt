fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
pydantic-settings>=2.2
httpx>=0.27
itsdangerous>=2.1
python-multipart>=0.0.9
websockets>=12.0
defusedxml>=0.7
# Optional: the LAN scan reads a snapshot's resolution with Pillow when present,
# and degrades gracefully (no resolution) when it is not.
pillow>=10.0
