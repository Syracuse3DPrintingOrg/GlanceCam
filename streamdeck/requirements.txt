# GlanceCam Stream Deck controller dependencies.
# Install into the same environment that runs the controller:
#   pip install -r streamdeck/requirements.txt

# Pure-Python driver for Elgato Stream Deck / Stream Deck Module hardware.
# 0.9.8 is the floor: it adds the USB product ids used by current XL / Module 32
# hardware revisions, which older releases fail to enumerate.
streamdeck>=0.9.8

# Key image rendering.
Pillow>=10.4.0

# HTTP client for the GlanceCam API.
httpx>=0.27.0
